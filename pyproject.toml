[project]
name = "magical"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "alandenadel-aifi", email = "alan.denadel@alleninstitute.org" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.1",
    "pandas>=3.0.5",
    "pytest>=9.1.1",
    "scipy>=1.18.0",
]

[build-system]
requires = ["uv_build>=0.8.18,<0.9.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "matplotlib>=3.11.1",
]
